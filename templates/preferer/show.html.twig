{% extends 'base.html.twig' %}

{% block title %}Preferer{% endblock %}

{% block body %}
    <h1>Preferer</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ preferer.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_preferer_index') }}">back to list</a>

    <a href="{{ path('app_preferer_edit', {'id': preferer.id}) }}">edit</a>

    {{ include('preferer/_delete_form.html.twig') }}
{% endblock %}
