<form method="post" action="{{ path('app_lier_delete', {'IdProfil': lier.IdProfil}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ lier.IdProfil) }}">
    <button class="btn">Delete</button>
</form>
