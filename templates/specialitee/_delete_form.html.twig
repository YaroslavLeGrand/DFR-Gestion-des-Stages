<form method="post" action="{{ path('app_specialitee_delete', {'id': specialitee.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ specialitee.id) }}">
    <button class="btn">Delete</button>
</form>
