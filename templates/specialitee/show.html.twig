{% extends 'base.html.twig' %}

{% block title %}Specialitee{% endblock %}

{% block body %}
    <h1>Specialitee</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ specialitee.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ specialitee.Libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_specialitee_index') }}">back to list</a>

    <a href="{{ path('app_specialitee_edit', {'id': specialitee.id}) }}">edit</a>

    {{ include('specialitee/_delete_form.html.twig') }}
{% endblock %}
