{% extends 'base.html.twig' %}

{% block title %}Salarie{% endblock %}

{% block body %}
    <h1>Salarie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ salarie.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ salarie.Nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ salarie.Prenom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_salarie_index') }}">back to list</a>

    <a href="{{ path('app_salarie_edit', {'id': salarie.id}) }}">edit</a>

    {{ include('salarie/_delete_form.html.twig') }}
{% endblock %}
