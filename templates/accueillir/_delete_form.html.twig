<form method="post" action="{{ path('app_accueillir_delete', {'IdClasse': accueillir.IdClasse}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ accueillir.IdClasse) }}">
    <button class="btn">Delete</button>
</form>
