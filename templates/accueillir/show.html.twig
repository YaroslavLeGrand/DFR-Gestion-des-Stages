{% extends 'base.html.twig' %}

{% block title %}Accueillir{% endblock %}

{% block body %}
    <h1>Accueillir</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Annee</th>
                <td>{{ accueillir.Annee ? accueillir.Annee|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_accueillir_index') }}">back to list</a>

    <a href="{{ path('app_accueillir_edit', {'IdClasse': accueillir.IdClasse}) }}">edit</a>

    {{ include('accueillir/_delete_form.html.twig') }}
{% endblock %}
